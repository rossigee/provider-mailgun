---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: webhooks.webhook.mailgun.m.crossplane.io
spec:
  group: webhook.mailgun.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mailgun
    kind: Webhook
    listKind: WebhookList
    plural: webhooks
    singular: webhook
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          This is the Crossplane v2 namespaced version.
          A Webhook is a managed resource that represents a Mailgun Webhook
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A WebhookSpec defines the desired state of a Webhook.
            properties:
              forProvider:
                description: WebhookParameters define the desired state of a Mailgun
                  Webhook
                properties:
                  domainRef:
                    description: DomainRef references the Domain this webhook belongs
                      to
                  domainSelector:
                    description: DomainSelector selects the Domain this webhook belongs
                      to
                  eventType:
                    description: EventType specifies the type of event this webhook
                      handles
                    enum:
                    - accepted
                    - delivered
                    - temporary_fail
                    - permanent_fail
                    - clicked
                    - opened
                    - unsubscribed
                    - complained
                    - stored
                    type: string
                  password:
                    description: Password for basic authentication (optional)
                    type: string
                  url:
                    description: URL is the callback URL for the webhook
                    pattern: ^https?://.*
                    type: string
                  username:
                    description: Username for basic authentication (optional)
                    type: string
                required:
                - domainRef
                - eventType
                - url
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A WebhookStatus represents the observed state of a Webhook.
            properties:
              atProvider:
                description: WebhookObservation reflects the observed state of a Mailgun
                  Webhook
                properties:
                  createdAt:
                    description: CreatedAt is when the webhook was created
                    type: string
                  domain:
                    description: Domain is the domain this webhook belongs to
                    type: string
                  eventType:
                    description: EventType specifies the type of event this webhook
                      handles
                    type: string
                  id:
                    description: ID is the webhook identifier in Mailgun
                    type: string
                  url:
                    description: URL is the callback URL for the webhook
                    type: string
                  username:
                    description: Username for basic authentication
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
