apiVersion: meta.pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-mailgun
  annotations:
    meta.crossplane.io/maintainer: Crossplane Contrib <info@crossplane.io>
    meta.crossplane.io/source: github.com/crossplane-contrib/provider-mailgun
    meta.crossplane.io/license: Apache-2.0
    meta.crossplane.io/description: |
      Crossplane provider for managing Mailgun resources including domains,
      mailing lists, routes, and webhooks.
    meta.crossplane.io/readme: |
      This Crossplane provider enables management of Mailgun resources through
      Kubernetes manifests. It supports both US and EU regions and provides
      comprehensive coverage of the Mailgun API.

      Features:
      - Domain management with DNS configuration
      - Mailing list creation and member management
      - Email routing rules
      - Webhook configuration for event notifications
      - Support for both US and EU Mailgun regions
spec:
  controller:
    image: harbor.golder.lan/library/provider-mailgun:v0.4.0
