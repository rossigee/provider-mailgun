apiVersion: bounce.mailgun.m.crossplane.io/v1beta1
kind: Bounce
metadata:
  namespace: default
  name: example-bounce
spec:
  forProvider:
    address: "bounced@example.com"
    code: "550"
    error: "5.1.1 User Unknown"
    domainRef:
      name: "example.com"
  providerConfigRef:
    name: default
