---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: templates.template.mailgun.m.crossplane.io
spec:
  group: template.mailgun.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mailgun
    kind: Template
    listKind: TemplateList
    plural: templates
    singular: template
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .spec.forProvider.domain
      name: DOMAIN
      type: string
    - jsonPath: .spec.forProvider.name
      name: TEMPLATE
      type: string
    - jsonPath: .status.atProvider.versionCount
      name: VERSIONS
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          This is the Crossplane v2 namespaced version.
          A Template is a managed resource that represents a Mailgun email template.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A TemplateSpec defines the desired state of a Template.
            properties:
              forProvider:
                description: TemplateParameters are the configurable fields of a Template.
                properties:
                  comment:
                    description: Comment for the initial version if template content
                      is provided.
                    type: string
                  description:
                    description: Description provides a human-readable description
                      of the template.
                    type: string
                  domain:
                    description: Domain is the domain this template belongs to.
                    type: string
                  engine:
                    default: mustache
                    description: Engine specifies the template engine to use.
                    enum:
                    - mustache
                    - handlebars
                    type: string
                  name:
                    description: Name is the template name identifier.
                    pattern: ^[a-zA-Z0-9._-]+$
                    type: string
                  tag:
                    description: Tag for organizing templates.
                    type: string
                  template:
                    description: Template contains the template content for the initial
                      version.
                    type: string
                required:
                - domain
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A TemplateStatus represents the observed state of a Template.
            properties:
              atProvider:
                description: TemplateObservation are the observable fields of a Template.
                properties:
                  activeVersion:
                    description: ActiveVersion contains information about the active
                      version.
                    properties:
                      active:
                        description: Active indicates if this is the active version.
                        type: boolean
                      comment:
                        description: Comment describing this version.
                        type: string
                      createdAt:
                        description: CreatedAt when this version was created.
                        type: string
                      engine:
                        description: Engine used for this version.
                        type: string
                      tag:
                        description: Tag identifying the version.
                        type: string
                    type: object
                  createdAt:
                    description: CreatedAt is when the template was created.
                    type: string
                  createdBy:
                    description: CreatedBy indicates who created the template.
                    type: string
                  description:
                    description: Description of the template.
                    type: string
                  name:
                    description: Name is the template identifier.
                    type: string
                  versionCount:
                    description: VersionCount is the number of versions for this template.
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
