---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: routes.route.mailgun.m.crossplane.io
spec:
  group: route.mailgun.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mailgun
    kind: Route
    listKind: RouteList
    plural: routes
    singular: route
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          This is the Crossplane v2 namespaced version.
          A Route is a managed resource that represents a Mailgun Route
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A RouteSpec defines the desired state of a Route.
            properties:
              forProvider:
                description: RouteParameters define the desired state of a Mailgun
                  Route
                properties:
                  actions:
                    description: Actions define what to do with messages matching
                      the expression
                    items:
                      description: RouteAction defines an action to take on matching
                        messages
                      properties:
                        destination:
                          description: |-
                            Destination is where to forward messages (for forward action)
                            Required for forward actions
                          type: string
                        type:
                          description: Type is the action type
                          enum:
                          - forward
                          - store
                          - stop
                          type: string
                      required:
                      - type
                      type: object
                    minItems: 1
                    type: array
                  description:
                    description: Description provides a human-readable description
                      of the route
                    type: string
                  expression:
                    description: Expression defines the filter for incoming messages
                    type: string
                  priority:
                    default: 0
                    description: Priority determines the order in which routes are
                      processed (0-100, lower = higher priority)
                    maximum: 100
                    minimum: 0
                    type: integer
                required:
                - actions
                - expression
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A RouteStatus represents the observed state of a Route.
            properties:
              atProvider:
                description: RouteObservation reflects the observed state of a Mailgun
                  Route
                properties:
                  actions:
                    description: Actions define what to do with messages matching
                      the expression
                    items:
                      description: RouteAction defines an action to take on matching
                        messages
                      properties:
                        destination:
                          description: |-
                            Destination is where to forward messages (for forward action)
                            Required for forward actions
                          type: string
                        type:
                          description: Type is the action type
                          enum:
                          - forward
                          - store
                          - stop
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                  createdAt:
                    description: CreatedAt is when the route was created
                    type: string
                  description:
                    description: Description provides a human-readable description
                      of the route
                    type: string
                  expression:
                    description: Expression defines the filter for incoming messages
                    type: string
                  id:
                    description: ID is the route identifier in Mailgun
                    type: string
                  priority:
                    description: Priority determines the order in which routes are
                      processed
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
